language: minimal

services:
    - docker

jobs:
    include:
        - stage: code quality (does nothing yet)
          script: true
        - stage: compile-lilypond
          script: pushd scores && make && popd
deploy:
    provider: script
    script: pushd scores && make deploy.sh && popd
    on:
        branch: master
